{
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/TronLink/mcp-tronlink-signer.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "publish:all": "pnpm -r build && pnpm -r publish --access public --no-git-checks"
  },
  "engines": {
    "node": ">=18"
  }
}
